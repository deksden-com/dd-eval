# PROTOCOLIZE entry — REV-054

SPECIFY is accepted and complete.

- `specify.json` preserves the confirmed priority taxonomy, defaults, constraints and acceptance criteria.
- The raw HITL question and answer are retained under `intake/hitl`.
- The run remains active with `start_protocolize` as its only next action.
- No protocol, feature workspace or product-code change exists before this boundary.
