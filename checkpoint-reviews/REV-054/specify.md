# SPECIFY entry — REV-054

Accepted entry for `cp-033-runtime-state-beta-112`.

- The project is a clean canonical checkout on `main`.
- The isolated runtime binds only engine `0.8.0-beta.109`.
- `RUN-001-task-priority` is unstarted at the SPECIFY boundary.
- The Subject completed ordinary project grounding and has not changed files.
- The persisted RUN state uses `dd-flow/flow-run@3`; legacy state mirrors are absent. `flow_guidance.current_stage` is an instruction, not duplicated RUN state.
