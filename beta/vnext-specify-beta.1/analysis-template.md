# Analysis: vnext-specify-beta.1 / <run-id>

## Frozen input

- Case/checkpoint:
- `dd-tasks` tag and commit:
- `dd-flow-cli` tag, commit, version, snapshot checksum:
- Controller session and worker session:

## Mechanical gate

- Engine and flow binding:
- Root Work / Agent Turn materialization:
- Trusted worker-session attachment:
- Receipt, schema validation, rendered result, and state transition:
- Forbidden downstream work:

Verdict: `valid | invalid_infrastructure_flow`.

## Semantic gate

- Grounding and context sufficiency:
- Requirements, acceptance, scope, constraints, and assumptions:
- Question quality or justified `specified` outcome:
- Handoff readiness for a separate protocolization session:

Verdict: `pass | fail`.

## Context misses and findings

List reviewer-observed misses and classify material findings as `engine`,
`flow`, `harness`, or `model`. State the narrowest next beta change, if any.
