{
  "name": "dd-eval",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "dd-eval": "./bin/dd-eval.mjs"
  },
  "scripts": {
    "test": "node --test",
    "validate": "node --test"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
