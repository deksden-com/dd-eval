{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "dd-eval/canonical-stage-checkpoint@2",
  "type": "object",
  "required": ["schema_id", "stage", "status"],
  "properties": {
    "schema_id": {"const": "dd-eval/canonical-stage-checkpoint@2"},
    "stage": {"enum": ["specify", "protocolize", "plan", "plan-review", "code"]},
    "status": {"enum": ["pending_capture", "accepted"]},
    "runtime_snapshot": {"type": ["object", "null"], "properties": {"locator": {"type": "string"}, "sha256": {"type": "string"}}},
    "subject": {
      "type": ["object", "null"],
      "properties": {"canonical_session_id": {"type": "string"}, "checkpoint_session_id": {"type": "string"}, "agent_id": {"type": ["string", "null"]}}
    }
  },
  "additionalProperties": true
}
